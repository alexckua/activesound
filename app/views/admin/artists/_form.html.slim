= semantic_form_for [:admin, @artist], builder: ActiveAdmin::FormBuilder do |f|
  = f.inputs do
    = f.input :title
    = f.input :description
    = image_tag @artist.avatar_url if @artist.avatar_url
    = f.input :avatar, as: :file
    = f.actions